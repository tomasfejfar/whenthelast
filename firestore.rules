service cloud.firestore {
  match /databases/{database}/documents {
    match /timers/{timer} {
      allow list, read, update, delete: if request.auth.uid == resource.data.uid
      allow create: if request.auth.uid == request.resource.data.uid;
    }
  }
}